@page
@model LoginModel
@{
    ViewData["Title"] = "Thay đổi lưu trữ";
}

<div class="max-w-md mx-auto bg-white p-8 rounded-lg shadow-md mt-12">
    <h2 class="text-2xl font-bold text-center text-blue-600 mb-6">Thay đổi Application</h2>

    <form method="post" class="space-y-6">
        <div>
            <label asp-for="ClientId" class="block text-sm font-medium text-gray-700">Client ID</label>
            <input asp-for="ClientId" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            <span asp-validation-for="ClientId" class="text-sm text-red-500"></span>
        </div>

        <div>
            <label asp-for="ClientSecret" class="block text-sm font-medium text-gray-700">Client Secret</label>
            <input asp-for="ClientSecret" type="password" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            <span asp-validation-for="ClientSecret" class="text-sm text-red-500"></span>
        </div>

        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="text-red-600 text-sm text-center">
                @Model.ErrorMessage
            </div>
        }

        <div class="text-center">
            <button type="submit" class="w-full bg-blue-600 text-white font-semibold py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
                Lưu
            </button>
        </div>
    </form>
</div>
